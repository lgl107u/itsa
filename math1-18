#include <stdio.h>
int main()
{
  int n, c, k;
  while(scanf("%d",&n)!=EOF)
  {
      for (c = 7; c >= 0; c--)
        {
            k = n >> c;

            if (k & 1)
            printf("1");
            else
            printf("0");
        }
      printf("\n");
  }

  return 0;
}
