#include <stdio.h>  
  
int main()  
{  
    int x;  
    while(scanf("%d",&x)!=EOF)  
    { 
        int result = 0;
        for(int i=1;i<x;i++){
            if(i%3==0 && i%2==0 && i%12!=0) result+=i;
        }
        printf("%d\n",result);
    }  
    return 0;  
}
