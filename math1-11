#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x;
    while (scanf("%d",&x)!=EOF)
    {
        int sp,lm,st;
        sp = x/10;
        x = x%10;
        lm = x/5;
        x = x%5;
        st = x;
        printf("NT10=%d\nNT5=%d\nNT1=%d\n",sp,lm,st);
    }
    
    return 0;
}
